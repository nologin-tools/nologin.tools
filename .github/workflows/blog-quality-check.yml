name: Blog Quality Check

on:
  pull_request:
    paths:
      - 'src/content/blog/**'
      - 'public/blog/images/**'

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - name: Validate blog post quality
        run: node scripts/validate-blog-post.mjs
